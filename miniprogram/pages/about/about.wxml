<view class="wrap">
  <view class="hero">
    <view class="hero-avatar">✈️</view>
    <view class="hero-title">常旅客助手</view>
    <view class="hero-sub">基于 RAG 的常旅客问答，答案有据可查</view>
  </view>

  <t-cell-group title="收录范围" theme="card">
    <t-cell wx:for="{{coverage}}" wx:key="group" title="{{item.group}}" note="">
      <view slot="note" class="tags">
        <t-tag wx:for="{{item.items}}" wx:for-item="it" wx:key="*this"
               theme="primary" variant="light" size="small">{{it}}</t-tag>
      </view>
    </t-cell>
  </t-cell-group>

  <t-cell-group title="免责声明" theme="card">
    <t-cell>
      <view slot="title" class="disc-text">
        答案由 AI 基于收录资料生成，可能存在过时、遗漏或错误。里程、积分、权益规则会不定期调整，
        涉及兑换、转点等操作前，请务必以航司、银行、酒店的官方渠道信息为准。
      </view>
    </t-cell>
  </t-cell-group>

  <view class="footer">ffp-rag · 常旅客 RAG</view>
</view>
