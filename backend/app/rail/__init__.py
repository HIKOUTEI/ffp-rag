"""铁路乘车记录模块。

两类数据严格分库（见 CONTEXT.md）：

- **共享参考数据** → `rail.db`：车站 / 车次 / 车次时刻表。只读，全体用户共用一份，
  由 `scripts.sync_gtfs` 每周整库重建并**原子替换**。
- **用户数据** → `journey.db`：乘车记录，挂内部 user_id（ADR-0007）。

**必须分库**：参考数据是可丢弃、周期性整体重建的；用户数据是不可重建的。
两者同库意味着每周的整库替换都会把用户行程放进爆炸半径里。
"""
