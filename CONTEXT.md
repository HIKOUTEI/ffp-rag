# ffp-rag —— 常旅客 RAG

常旅客（里程/积分/信用卡/酒店）领域的检索增强问答系统。后端为 FastAPI + Chroma
向量库，多个前端（网页 React、微信小程序、未来 iOS）共享同一套无状态 HTTP API。

小程序下另有两条**不属于问答**的产品线：**铁路乘车记录**（见下方「乘车记录」及
ADR-0007、ADR-0008）与**奖赏钱记录**（见下方「奖赏钱」及 ADR-0009、ADR-0010）。
它们与 RAG 知识库互不相通——铁路**不是**常旅客问答的领域范畴，也**不是** `domain`
的取值；奖赏钱记录是用户本人的账本，`credit_card` 那个 `domain` 指的是信用卡**知识**，
与账本无关。

## Language

**常旅客 / FFP**：
Frequent Flyer Program。本项目的领域范畴：航司里程与会籍权益、信用卡积分转点、酒店积分互通。

**域 / domain**：
一条知识片段所属的领域分类，取值 `airline` / `credit_card` / `hotel` / `personal_account` / `other`。
_Avoid_: 分类、category

**片段 / Fragment**：
入库到向量库的最小知识单位，含 `domain`、`source`、`text`、`url`。检索与引用都以片段为单位。
_Avoid_: chunk、文档、段落

**来源 / Source**：
一条回答所引用片段的出处标签（`domain` + `source` + 相似度 `score`）。前端展示以建立可信度。
_Avoid_: 引用、reference、出处链接

**追问改写 / Query Rewrite**：
把依赖上下文的追问（如「那金卡呢」）结合历史改写成独立完整、可直接检索的问题。
_Avoid_: 补全、问题扩展

**会话 / Conversation**：
一轮多轮对话的完整消息序列（`messages`，每条含 `role` 与 `content`）。会话历史由前端
每次请求携带（问答本身无服务端会话状态）；但用户身份与每日额度是服务端状态（见「登录态」）。
_Avoid_: session、聊天记录、上下文

**登录态 / Auth**：
小程序 `wx.login` 拿 code，后端 `code2session` 换 openid 并签发 token；`/chat` 系列
须带 `Authorization: Bearer <token>`，按 openid 限每日额度。管理接口另用 `ADMIN_TOKEN`。
_Avoid_: 鉴权、session、账号

**问题模板 / Question Template**：
一组预先定义的规范问法，既是冷启动的示例问题，也是热门榜的统计与展示单位。
用户的每次真实提问会用 embedding 归到相似度最近的模板上。
_Avoid_: 预设问题、示例、FAQ

**热门榜 / Popular Questions**：
按累计命中次数排序的问题模板列表，供前端首屏空状态展示。冷启动（计数为空）时
回退到模板的预设顺序。
_Avoid_: 排行榜、热搜、推荐问题

**规范化 URL / Canonical URL**：
把同一篇文章的各种链接变体（追踪参数、`#` 片段、大小写差异）折叠成唯一形式后的 URL。
判断「是否已录入」以规范化 URL 为准。公众号 `mp.weixin.qq.com` 仅保留 `__biz/mid/idx/sn`。
_Avoid_: 原始链接、去重 key、URL 指纹

**已录入 / Ingested**：
一篇文章的规范化 URL 已有片段真正写入向量库（`ingest-parsed` 成功）。
仅「解析过但未入库」不算已录入，可重新解析。判定以向量库中是否存在该 URL 的片段为准。
_Avoid_: 已解析、已抓取、已处理

**用户档案 / User Profile**（见 ADR-0003）：
用户在小程序「我的」页维护的个人常旅客资产，含三类**条目 / Entry**：航司会籍、
酒店会籍、信用卡。仅存前端 `Storage`，后端不参与。每条只记相对稳定的信息
（品牌 + 等级/卡种 + 可选标识号），**不含里程/积分余额**。
_Avoid_: 账户、user account、会员数据（`personal_account` 是知识库的域取值，与此无关）

**纠错记录 / Correction**（见 ADR-0005）：
用户对某条 AI 回答的报错，含当时的问答快照与命中的**知识条目 id**。它是给管理员的
待办项，不自动改库；管理员据此修正知识后手动标记已处理。用户侧的动作叫「报错」，
后台那一屏叫「纠错队列」。
_Avoid_: 错题本、反馈、feedback、bug（「知识条目」无关联时表示库里缺这条知识）

**会籍 / Membership**：
用户档案中「航司」或「酒店」类条目，含所属品牌与**等级 / Tier**（如 万豪钛金、
国航白金）。等级取自内置「品牌 → 等级清单」预置表。
_Avoid_: 等级卡、身份、status（中文统一用「会籍/等级」）

**用户 / User**（见 ADR-0007）：
服务端的内部账号，主键 `user_id`。通过**身份 / Identity**（`provider` + 外部标识，
现在只有微信 openid）关联到具体登录方式。用户业务数据一律挂 `user_id`，不挂 openid。
_Avoid_: 账号、openid（openid 是「登录态」里的微信侧标识，不是本项目的用户主键）

**车次 / Train Number**：
`G1`、`K551/K554` 这样的列车编号本身。约 29% 的车次因跨线换号而含 `/`，
而用户车票上只印其中一段，故检索需按 `/` 分段匹配。
_Avoid_: 车号、班次号、trip_id

**车次时刻表 / Timetable**：
一个车次的标准停站序列，含各站到发时刻与累计营业里程。**与日期无关**——
数据源不含开行日历，无法判断某车次某天是否开行。全体用户共用一份只读参考数据。
_Avoid_: 班次、运行图（「运行图」专指快照）、时刻表版本

**停站 / Stop**：
车次时刻表中的一站，含序号、车站、到达/发车时刻、自始发起的累计里程。
时刻可跨日（实测最大第 4 天），故以「自始发 00:00 起的分钟数」表达而非时分。
_Avoid_: 站点、经停、车站（「车站」是实体，「停站」是车次在该实体上的一次停靠）

**车站 / Station**：
铁路车站实体，含站名与 WGS84 坐标。**无电报码**（数据源不提供）。
同城多站以全名区分（北京站/北京南/北京西…）。另带**城市/省份归属**，
由坐标反查行政区划得到——站名不可靠（朝阳镇在通化，不在朝阳）。
_Avoid_: 站点、stop（`stop` 在本项目专指「停站」）

**乘车记录 / Journey**（见 ADR-0007）：
用户实际乘坐的**一段**，= 车次 + 乘车日期 + 上车站 + 下车站。用户坐的通常不是全程
（G1 全程 7 站，可能只坐第 2 站到第 5 站）。属于用户本人，存服务端。
补录的历史行程可手填，此时**不编造时刻**。
_Avoid_: 行程（易与 GTFS 的 `trip` 混淆）、车票、订单、打卡

**运行图快照 / Timetable Snapshot**：
每周归档的一份完整时刻表原始数据，以版本号标识。每条乘车记录记下录入时所用的版本，
使未来仍能还原「当时那趟车是什么时刻」。
_Avoid_: 备份、历史版本、时刻表（「时刻表」指当前生效的那份）

**奖赏钱 / RewardCash（RC）**：
香港汇丰信用卡的回赠单位，$1 RC = HK$1。属于**整个汇丰账户而非某一张卡**——
名下所有汇丰卡共用同一份余额与同一份年度上限。
_Avoid_: 积分、现金回赠、cashback（澳门汇丰的「奖赏积分」是另一套，不通用）

**月结记录 / Monthly Entry**（见 ADR-0010）：
用户每月照着 Reward+ App 抄一次的记录。含两类**性质不同**的字段：
**流量**（当月各类别赚取的 RC，可跨月加总）与**存量**（期末余额、即将到期 RC，
是快照，**不可加总**，只取最新一条）。二者必须在类型上分开。
_Avoid_: 账单、对账单、statement（汇丰的「月结单」是另一回事）

**签账记录 / Spend Entry**：
可选的逐笔消费记录。唯一不可替代的用途是**月中推算门槛进度**——月结次月才抄，
那时门槛周期已结束。与月结是**校准关系**：月结到账后以月结为准，两者不相加。
_Avoid_: 交易、流水、订单

**奖赏类别 / Reward Category**：
Reward+ App 自己那套赚取来源分类（最红自主奖赏／信用卡消费／优惠／Well+…）。
分**费率型**（可由 RC ÷ 费率反推签账额）与**定额型**（优惠、迎新，除以任何费率都无意义）。
_Avoid_: 分类、category（`domain` 是知识库的分类，与此无关）

**返现规则 / Rebate Rule**（见 ADR-0009）：
一条可赚取奖赏钱的规则，含费率或定额、上限、周期口径、门槛、有效期、条款出处。
全局只读，由后端下发，管理员在控制台维护。
_Avoid_: 优惠、活动、promo

**签账上限 / Spend Cap** 与 **奖赏上限 / Reward Cap**：
两种不同的封顶。签账上限限的是**消费额**（最红自主奖赏首 HK$100,000），
奖赏上限限的是**拿到的 RC**（内地餐饮每月 $80 RC）。二者不可混算。
_Avoid_: 上限、封顶（必须指明是哪一种）

**门槛 / Threshold**：
触发某条规则的前置条件，**不是上限**。如内地餐饮 +3% 要求
「每历月内地累计合资格签账 ≥ HK$1,200」——没到一分不给，到了才开始算。
_Avoid_: 条件、最低消费（「上限」是反向概念，别混用）

**周期口径 / Cap Period**：
上限的重置周期，取值 `历年` / `历月` / `促销期` / `会籍年`。四种同时存在且起止日各异，
故必须逐条显式记录起止日期。**不指明口径的「年」在本项目中无意义。**
_Avoid_: 年度、周期

**登记 / Enrolment**：
部分规则要求用户先在 Reward+ App 主动登记才开始计算，**登记前的签账不补算**。
是否登记记在用户侧（未问过／确认未登记／已登记+日期，三态）。
_Avoid_: 开通、激活、报名

**确信度 / Confidence**：
一条返现规则的可靠程度：`official` 条款明写 ／ `user_verified` 实测到账确认 ／
`unverified` 由商户 MCC 当场决定、无法预判 ／ `unavailable` 条款确认但登记窗口未开。
_Avoid_: 状态、可信度

